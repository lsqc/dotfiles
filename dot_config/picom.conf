# disable fading
fading = false;

# set backend
backend = "glx"

# disable vsync
vsync = false

# blur
blur-method = "dual_kawase";
blur-strength = 1;
blur-background = true;

# set opacity
opacity-rule = [
	"95:class_g = 'Alacritty'", # terminal
]

# exclude notifications from shadows
shadow-exclude = [
	"name = 'Notification'"
]

